{
  "buildCommand": "composer install --optimize-autoloader --no-dev",
  "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
  "webCommand": "serve public",
  "installCommand": "npm install && npm run build"
}